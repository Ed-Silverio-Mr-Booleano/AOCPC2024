#include <bits/stdc++.h>
#define fast ios_base::sync_with_stdio(NULL), cin.tie(NULL)

using namespace std;
int main(){
  fast;
  vector<int> a(10);

  for(int &x: a)
    cin>>x;

  int c = 0;
  c = a[c];
  c = a[c];
  c = a[c];
    
  cout<<c<<endl;
}